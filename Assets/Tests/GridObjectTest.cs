using System.Collections;
using System.Collections.Generic;
using Data;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

namespace Tests {
    public class GridObjectTest {
        // [Test]
        // public void IndexShouldGoBackAndForthForSameBound() {
        //     {
        //         var bounds = new BoundsInt(0, 0, 0, 10, 10, 1);
        //         var position = new Vector3Int(0, 0, 0);
        //         var resultIndex = GridObject.ToIndex(position, bounds);
        //         var resultPosition = GridObject.FromIndex(resultIndex, bounds);
        //         Assert.AreEqual(0, resultIndex);
        //         Assert.AreEqual(position, resultPosition);
        //     }
        //     {
        //         var bounds = new BoundsInt(-5, -1, 0, 10, 10, 1);
        //         var position = new Vector3Int(5, 5, 0);
        //         var resultIndex = GridObject.ToIndex(position, bounds);
        //         var resultPosition = GridObject.FromIndex(resultIndex, bounds);
        //         Assert.AreEqual(110, resultIndex);
        //         Assert.AreEqual(position, resultPosition);
        //     }
        // }

        // A UnityTest behaves like a coroutine in Play Mode. In Edit Mode you can use
        // `yield return null;` to skip a frame.
        // [UnityTest]
        // public IEnumerator GridObjectTestWithEnumeratorPasses() {
        // Use the Assert class to test conditions.
        // Use yield to skip a frame.
        // yield return null;
        // }
    }
}